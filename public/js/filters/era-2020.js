// 2020s: Cinema HD
// Style: Clean, No Filters, Pure Quality

export function apply(ctx, width, height, time) {
    // Pass-through. The 'canvas.js' already drew the raw frame.
    // We do nothing here to preserve maximum quality.
    // If you wanted to force a "Cinematic" aspect ratio (2.35:1) we could add black bars,
    // but the prompt asked for the "2020" mode which is high quality.
}